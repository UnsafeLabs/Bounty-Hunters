import React, { useState, useRef, useEffect, useCallback } from "react";
import { ChatView } from "./ChatView";
const ChatView = () => {
  const [messages, setMessages] = useState<ChatMessage[]>([]);
  const [input, setInput] = useState("");
  const [isSending, setIsSending] = useState(false);
  const [isError, setIsError] = useState(false);
  const [error, setError] = useState("");
  const [isAtBottom, setIsAtBottom] = useState(true);
  const messagesEndRef = useRef<null | HTMLDivElement>(null);
  const scrollToBottom = useRef<null | HTMLDivElement>(null);
  const [showPicker, setShowPicker] = useState(false);
  const [showPickerItem, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPickerItem, setShowPickerItem] = useState(false);
  const [showPicker, setShowPicker] = useState(false);
  const [showPickerItem, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPickerItem, setShowPickerItem] = useState(false);
  const [showPicker, setShowPicker] = useState(false);
  const [showPickerItem, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPickerItem, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPicker, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPickerItem, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPicker, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPickerItem, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPicker, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPickerItem, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPicker, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPickerItem, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPicker, setShowPicker] = useState<null | HTMLDivElement>(null);
  const [showPickerItem, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPickerItem, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPicker, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPickerItem, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPicker, setShowPicker] = useState<null | HTMLDivElement>(null);
  const [showPickerItem, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPicker, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPickerItem, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPicker, setShowPicker] = useState<null | HTMLDivElement>(null);
  const [showPickerItem, setShowPickerItem] = useState(false);
  const [showPicker, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPickerItem, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPicker, setShowPicker] = useState<null | HTMLDivElement>(null);
  const [showPickerItem, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPicker, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPickerItem, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPicker, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPickerItem, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPicker, setShowPicker] = useState<null | HTMLDivElement>(null);
  const [showPickerItem, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPicker, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPickerItem, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  (state, and) = useState<null | HTMLDivElement>(null);
  const [showPicker, setShowPicker] = useState<null | HTMLDivElement>(null);
  const [showPickerItem, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPicker, setShowPicker] = useState<null | HTMLDivElement>(null);
  const [showPickerItem, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPicker, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPickerItem, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPicker, setShowPicker] = useState<null | HTMLDivElement>(null);
  const [showPickerItem, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPicker, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPickerItem, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPicker, setShowPicker] = useState<null | HTMLDivElement>(null);
  const [showPickerItem, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPicker, setShowPickerItem] = useState<null | HTMLDivElement>(null);
  const [showPickerItem, setShowPickerItem] = useState<null | HTML