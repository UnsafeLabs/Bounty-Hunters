 Just the code. Only one block of code. Just the answer. No markdown. Just the solution. Only the answer. Only the code. No explanation. Just the answer. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code. Only the answer. Only the code.