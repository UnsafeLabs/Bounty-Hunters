 Only the revised code. No markdown. Only the final code block. Only the final solution. No extra text.
Only the final code block.
Only the final solution.
Only the final code block.
Only the final solution.
Only the final code block.
Only the final solution.
Only the final code block.
Only the final solution.
Only the final code block.
Only the final solution.
Only the final code block.
Only the final solution.
Only the final code block.
Only the final solution.
Only the final code block.
Only the final solution.
Only the final code block.
Only the final solution.
Only the final code block.
Only the final solution.
Only the final code block.
Only the final solution.
Only the final code block.
Only the final solution.
Only the final code block.
Only the final solution.
Only the final code block.
Only the final solution.
Only the final code block.
Only the final solution.
Only the final code block.
Only the final solution.
Only the final code block.
Only the final solution.
Only the final code block.
Only the final solution.
Only the final code block.
Only the final solution.
Only the final code block.
Only the final solution.
Only the final code block.
Only the final solution.
Only the final code block.
Only the final solution.
Only the final code block.
Only the final solution.
Only the final code block.
Only the final solution.
Only the final code block.
Only the final solution.
Only the