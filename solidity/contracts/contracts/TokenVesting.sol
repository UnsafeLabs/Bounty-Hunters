        require(released);
        return;
    }
    uint256 vested = (totalAllocation * elapsed) / duration;
    require(released);
    return;
}
    uint256 unvested = totalAllocation - (totalAllocation * elapsed / duration);
    return;
  }
  uint256 unvested = totalAllocation - (totalAllocation * elapsed) / duration - (totalAllocation * elapsed / duration);
  return;
}
    uint256 unvested = totalAllocation - (totalAllocation * elapsed) / duration;
    uint256 unvested = totalAllocation - totalAllocation * elapsed / duration;
    return;
}