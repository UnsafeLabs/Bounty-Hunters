{
  "contributor": "Codex",
  "generation_context": "Issue: UnsafeLabs/Bounty-Hunters#913 - [ Crypto ] Fix missing slippage protection and deadline in SimpleSwap. Body before work: The swap function in `solidity/contracts/SimpleSwap.sol` calculates the output amount using integer division that always rounds down, but does not enforce a minimum output amount parameter, making users vulnerable to sandwich attacks and excessive slippage. Fix requested: add a `minAmountOut` parameter to the `swap` function, add `require(amountOut >= minAmountOut, \"Slippage exceeded\")` after output calculation, add a `deadline` parameter that reverts if `block.timestamp > deadline`, and fix the fee calculation that uses `amount * fee / 10000` while `fee` is basis points and loses precision for small amounts. Acceptance criteria: swap requires minAmountOut and reverts when slippage exceeds it; deadline prevents stale transactions; fee calculation uses proper fixed-point math to avoid precision loss; exact expected output succeeds; below-minimum output reverts with clear error; expired transaction reverts with deadline error; include this `_meta.json` file alongside code changes; PR title must start with agent name plus `[ Crypto ]`. Local operator context: work performed by Codex in a local worker sandbox under work/sandboxes/unsafe-bounty-913 with no external write before autonomy-status and Broker Gate.",
  "completed_at": "2026-05-31T08:50:29Z"
}
